# Planto — Website

Static marketing site. No build step.

## Preview

```
python3 -m http.server 8000
```

Then open http://localhost:8000.

## Deploy

Drop the whole directory onto any static host (GitHub Pages, Netlify, Cloudflare Pages, S3 + CloudFront). No build, no env vars.
